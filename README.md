# App Engine Standard Flask for Hackaton

    pip install -t lib -r requirements.txt

then to deploy:

    gcloud app deploy

Copy `credentials_sample.ini` to `credentials.ini` and replace placeholder with real keys (no quotes required).
